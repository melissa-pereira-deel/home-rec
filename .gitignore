# Xcode
**/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.xcuserstate

# Build artifacts
build/
dist/
*.app
*.dSYM
*.dSYM.zip
*.xcresult
*.dmg
*.xcarchive

# Swift Package Manager
.build/
Packages/
Package.pins
# Package.resolved is deliberately NOT ignored. That is the convention for a
# *library*, whose consumers resolve their own versions — this is an app, and
# the lockfile is the only record of which dependency version a release was
# actually built from. It matters most for Sparkle (BL-034): an auto-updater
# resolved at build time to "whatever is newest" is a supply-chain surface on
# the one component that can replace the app on a user's disk.

# Code signing
*.mobileprovision
*.provisionprofile

# Claude Code
.claude/
CLAUDE.md

# macOS
.DS_Store
.AppleDouble
.LSOverride
**/.DS_Store

# Temporary files
*.tmp
*~
*.swp
*.swo

# Audio test files
test_recordings/

# ─── Product management / internal (NEVER publish) ───
# All internal docs live under private/. The folder is the structural
# guarantee: anything here physically cannot be committed.
/private/
BACKLOG.md
docs/product-strategy/
*.private.md

# ─── Marketing artifacts (keep local, publish via website host) ───
home-rec-website.html
privacy.html
terms.html
favicon.svg
favicon.ico
apple-touch-icon.png

# The website is its own repo (home-rec-site) and deploys from there.
# A local copy here would be a second, silently-diverging source of truth.
home-rec-site-main/

# Vercel deploy config — describes how the website is deployed, not how the
# app is built. Kept local alongside the website HTML files.
vercel.json
.vercelignore
.vercel/

# Build logs from scripts/build-dmg.sh (contain local paths + submission IDs)
dist-build.log

# ─── Environment & secrets (NEVER publish) ───
.env
.env.*
*.pem
*.p12
*.cer
*.p8
*.key
*_private_key*
AuthKey_*.p8
secrets.*
notariz*.txt
*appcast*.key
.vercel
.env*
*.profraw
